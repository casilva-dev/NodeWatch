# Terminal Screen Size (Optional)
#SIZE_SCREEN=185x46

# Bitcoin core folder path (Default: ~/.bitcoin)
#BITCOIN_DATADIR=/mnt/bitcoin

# Sets the fee forecast method:
# 'smart' uses the embedded 'estimatesmartfee' from bitcoin-cli
# 'median' to find the median block fee for transactions in the mempool
# 'mempool' uses the recommended fees from the mempool.space API
# 'auto' randomly selects between 'smart', 'median', and 'mempool' methods
#FEES_FORECAST=auto

# Fee API URL used when FEES_FORECAST=mempool
#FEES_API_URL=https://mempool.space/api/v1/fees/precise

# Fullnode storage device (Default: the device that holds the bitcoin core directory.)
#IOSTAT_DEVICE=/dev/sda1
#IOSTAT_DEVICE=/dev/disk/by-uuid/3f102c82-3118-4867-8aa9-6d30a167a4c4
